. 출발 전 – Logic 1.1/1.2 + 혼잡도 (C-1)

사용자 설정
집/회사 좌표 + First/Last Mile + 목표 도착 시각을 /users/me/settings/commute에 저장.
앱이 아침 7:30에 Logic 1.1을 호출:
ODSAY로 좌표 기반 경로 검색 → 가장 빠른 경로(path[0]) 선택.
서울 지하철 ETA/버스 ETA로 arrivalMinutes(정류장 기준 도착까지 남은 시간) 계산.
서울교통공사 혼잡도 데이터로,
“지금 시간대 남구로→부천 구간 혼잡도: 85% = HIGH” 같은 값을 가져옴.
Door-to-Door 계산:
totalDurationMinutes = First Mile + 대기시간 + Transit + Last Mile
departureInMinutes = First Mile + 대기시간 (집 기준, 몇 분 뒤에 타게 되는지)
슬랙 계산 후 메시지:
예:
GO_NOW + 혼잡도 보통:
“8:50 도착을 위해, 지금 집에서 출발하셔서 12분 후 도착하는 [7호선 구로09]를 타세요. (현재 혼잡도: 보통)”
LAST_CHANCE + 혼잡도 높음:
“⚠️지각 주의! ‘마지막 지하철[7호선]’이 7분 뒤 도착합니다. (현재 혼잡도: 매우 높음)”
→ 이 단계에서 혼잡도는 “경험의 질” Gate + 메시지 텍스트에 사용.

2. 이동 중 – Logic 2.2/2.3/2.4 + 혼잡도 (C-2 일부)

사용자가 기본 경로로 이동 중:

Logic 2.1이 GPS로 WAITING/WALKING/ON_TRIP 상태를 감지해 화면을 자동 전환.
Logic 2.2 (대안 경로) 호출 시:
Gate 1: 시간 이득 (ODSay + ETA)
Gate 2: 환승 여유시간 (다음 열차/버스 ETA)
Gate 3: 혼잡도 (서울교통공사 평균 혼잡도 기준 HIGH 이상이면 경험 페널티)
예: “10분 빨라지지만 현재 혼잡도 95% → 추천 보류” 같은 판단.
Logic 2.3 (칸 추천)은 1단계에서는
“역/구간 전체 혼잡도”까지만 보고,
칸별 추천은 PUZZLE 칸 혼잡도 데이터가 들어오는 Phase 2.3에서 확장.
Logic 2.4 (환승 리마인더):

환승역 반경 500m 진입 시,
다음 환승 열차 ETA + 해당 구간 혼잡도(HIGH/MED/LOW)를 같이 안내.
3. 돌발 상황/마지막 대안 – Logic 3.1/3.2 (C-2/C-3)

Logic 3.1 지연 감지:

AverageDurationDB의 평균 소요시간 + 도로/TPEG or 실시간 ETA 로 “예상 vs 평균” 비교.
동시에 해당 구간의 평균 혼잡도가 높으면,
“평소에도 붐비는 구간 vs 오늘만 특이하게 막힌 구간”을 구분.
메시지:
“⚠️지연 감지! [A정류장→B정류장] 구간이 평소보다 8분 늦고, 현재 혼잡도도 매우 높습니다.”
Logic 3.2 택시 제안:

지각 확정 / 막차 끊김 + 혼잡도·ETA를 종합해서:
“대중교통 ETA 35분, 혼잡도 HIGH / 택시 ETA 18분, 요금 12,000원 → 택시 제안”
여기서 택시 ETA/요금은 나중에 선택한 모빌리티 API(C-3)에서 가져와 비교.

재난 알림 odsay로 받은 경로를 기반으로 그쪽의 재난 문자가 나오면 보여준다. 