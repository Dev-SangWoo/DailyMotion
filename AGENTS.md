# 데일리모션 프로젝트 헌법 (AGENTS.md)

> 이 문서는 '데일리모션' 프로젝트의 '단 하나의 진실(SSOT)'입니다.

이 프로젝트에 참여하는 모든 개발자(AI 포함)는 코드를 작성하기 전 이 헌법을 반드시 숙지하고 준수해야 합니다. 모든 '바이브'는 이 '헌법'의 테두리 안에서 이루어져야 합니다.

**문서 구분**:
- **README.md**: 프로젝트 '손님'을 위한 안내서 (설치, 실행법)
- **AGENTS.md**: 프로젝트 '건축가'를 위한 법전 (모든 규칙, 아키텍처)

## 프로젝트 개요

데일리모션 프로젝트의 핵심 규칙과 가이드라인을 정의합니다.

## 아키텍처 원칙

### 1. 프론트엔드 (client/)
- **기술 스택**: React Native
- **위치**: `client/src/`
- **설명**: 모바일 애플리케이션 프론트엔드

### 2. 백엔드 (server/)
- **기술 스택**: FastAPI (Monolithic Architecture)
- **위치**: `server/app/`
- **설명**: 모놀리식 아키텍처 기반 백엔드 서버

### 3. 문서화 (docs/)
- **OpenAPI 스펙**: `docs/openapi/v1.yaml`
- **자동 생성 파일**: `docs/openapi/generated/`
- **모듈 설계서**: `docs/MODULES.md`
- **설명**: API 설계도, 모듈 설계서 및 자동 생성된 클라이언트/서버 코드

## 개발 규칙

1. 모든 API 변경사항은 OpenAPI 스펙에 반영되어야 합니다.
2. 코드 스타일과 컨벤션은 일관되게 유지합니다.
3. 문서화는 코드와 함께 업데이트되어야 합니다.

---

## 1. 🏗️ 프론트엔드 헌법 (React Native)

클라이언트 애플리케이션의 구조와 규칙을 정의합니다.

### [제1장] 상태 관리 (신경계)

**제1조 (기술)**: 전역 상태 관리는 Zustand를 사용한다.

**제2조 (시행령)**:
- 모든 스토어(Store)는 `src/stores/` 디렉토리에서 `use[Name]Store.ts` 형태로 관리한다.
- 상태를 변경하는 로직(Action)은 스토어 내부의 actions 객체 안에서 명시적으로 정의한다.
- 서버 상태(API 응답)는 Zustand 스토어에 저장하는 것을 **'원칙적 금지'**한다. (제3장 React Query 참조)

### [제2장] 스타일링 (피부)

**제1조 (기술)**: 모든 UI 스타일링은 Styled-components를 사용한다.

**제2조 (시행령)**:
- `src/styles/theme.ts` 에 정의된 '중앙 테마(Theme)' (색상, 폰트, 간격)를 반드시 사용해야 한다. 매직 넘버(예: `color: '#FFF'`) 사용을 금지한다.
- 스타일 컴포넌트는 재사용성과 '의미론적' 이름을 갖도록 설계한다. (예: `Button` O, `BlueBox` X)

### [제3장] 데이터 페칭 (혈액)

**제1조 (기술)**: 모든 서버 API 통신(서버 상태)은 **React Query (TanStack Query)**를 통해 관리한다.

**제2조 (시행령)**:
- `useEffect` 와 `useState` 를 조합한 수동 API 호출을 **'엄격히 금지'**한다.
- `isLoading`, `isError`, `data` 상태는 React Query가 제공하는 것을 사용한다.
- `src/hooks/queries/` 디렉토리에서 기능별로 커스텀 훅(`useGet[Name]Query`)을 만들어 사용한다.

### [제4장] 내비게이션 (도로망)

**제1조 (기술)**: 모든 화면 이동(내비게이션)은 React Navigation을 사용한다.

**제2조 (시행령)**:
- 모든 스크린(Screen)은 `src/screens/` 디렉토리에서 관리한다.
- 내비게이션 스택과 스크린 이름, 파라미터 타입은 `src/navigators/` 디렉토리에서 중앙 관리한다.

### [제5장] 성능 최적화 (v3.0 시행령)

**제1조 (배터리 보호)**: '스마트 폴링 (Smart Polling)' 로직을 구현한다.

**제2조 (시행령)**:
- 사용자 상태에 따라 GPS/API 호출 빈도를 동적으로 조절한다.
- '순항 구간'(지하철)에서는 Low Frequency, '환승 지점' 접근 시 High Frequency로 작동하여 '배터리 광탈' 함정을 방지한다.

### [제6장] 개발 방법론 (방패)

**제1조 (원칙)**: **TDD (Test-Driven Development)**를 핵심 개발 방법론으로 채택한다.

**제2조 (기술)**: Jest와 **React Native Testing Library (RTL)**를 사용한다.

**제3조 (시행령)**:
- 모든 핵심 '사용자 경험'과 '비즈니스 로직'은 코드를 작성하기 전 RTL 테스트 케이스로 '먼저' 정의한다.
- 테스트는 '구현'이 아닌 '사용자 관점'(예: "사용자는 '지각 주의' 텍스트를 볼 수 있는가?")에서 작성하여, '지속가능한' 리팩토링을 보장한다.

---

## 2. 🌳 백엔드 헌법 (Python / FastAPI)

서버 애플리케이션의 구조와 규칙을 정의합니다.

### [제1장] 아키텍처

**제1조 (구조)**: 백엔드는 모놀리식(Monolithic) 아키텍처를 채택한다.

**제2조 (시행령 - "모듈형 모놀리식")**:
- 모놀리식의 '스파게티 코드' 함정을 피하기 위해, 코드는 **'가상의 마이크로서비스'**처럼 '모듈(폴더)'로 완벽하게 분리한다.
- `server/app/` 하위에 `path_optimize/`, `ai_pattern/`, `risk_manage/` 등 기능 단위로 폴더를 분리한다.
- 각 모듈은 다른 모듈의 내부 구현을 직접 참조할 수 없으며, 정의된 '서비스 레이어'를 통해서만 통신한다.

**제3조 (핵심 시행령 - v3.0)**: 모든 모듈은 v3.0 명세서의 비즈니스 로직을 구현하는 것을 최우선 목표로 한다.
- '문 앞(Door-to-Door)' 기준을 준수하며, 'First/Last Mile' 도보 시간을 계산에 포함한다.
- **'출근 모드(지각 방지)'**와 **'퇴근 모드(경험 충족)'**를 명확히 분리하여 로직을 수행한다.
- `path_optimize` 모듈은 [Logic 2.2]의 3가지 엄격한 조건(Gate)(확실한 이득, 환승 확정성, 경험의 질)을 통과하지 못한 '쓸데없는 제안'을 사용자에게 보내는 것을 **'엄격히 금지'**한다.
- `ai_pattern` 모듈은 [Logic 3.1]의 핵심 자산인 '구간별/시간대별 평균 소요시간 DB' 를 구축하고 관리할 책임을 진다.

### [제2장] 기술 스택

**제1조 (프레임워크)**: Python / FastAPI 를 사용한다.

### [제3장] 데이터베이스 (주춧돌)

**제1조 (기술)**: PostgreSQL을 메인 RDB로 사용한다.

**제2조 (확장)**: '데일리모션'의 핵심 GIS 기능을 위해 PostGIS 확장을 반드시 사용한다.

**제3조 (시행령)**: '세이프티 가드'의 '공간 교차 질의'는 `ST_Intersects` 등 PostGIS의 내장 함수를 활용해 구현한다.

**제4조 (v3.0 시행령)**: **'구간별/시간대별 평균 소요시간'**을 저장, 집계, 조회하기 위한 '통계 테이블'을 `ai_pattern` 모듈의 책임 하에 설계 및 운영한다.

### [제4장] 클라우드 (인프라)

**제1조 (제공사)**: 모든 인프라는 NHN Cloud 를 기반으로 구축한다.

**제2조 (시행령)**:
- 애플리케이션 배포는 NHN Cloud Kubernetes Service (NKS)를 활용한다.
- '시민 리포트'의 사진/영상 저장은 NHN Cloud Object Storage를 활용한다.
- DB는 NHN Cloud for PostgreSQL (관리형 서비스) 사용을 최우선으로 검토한다.
- [Logic 3.1]의 'AI 검증' 로직은 NHN Cloud의 AI/ML 서비스(Vision, NLP)를 호출하여 구현한다.

### [제5장] 개발 방법론 (방패)

**제1조 (원칙)**: **TDD (Test-Driven Development)**를 핵심 개발 방법론으로 채택한다.

**제2조 (기술)**: Pytest를 테스트 프레임워크로 사용한다.

**제3조 (시행령)**:
- 모든 비즈니스 로직(v3.0 명세서의 Logic 1.1, 1.2 등)은 코드를 작성하기 전 Pytest 케이스로 '먼저' 정의한다.
- 테스트 케이스는 OpenAPI '설계도'와 v3.0 '엔진 설계도'의 약속을 '영원히' 보장하는 '자동화된 집행관' 역할을 수행한다.

---

## 3. 🤝 상호 규약 (The Border)

프론트엔드와 백엔드 간의 '국경' 규칙을 정의합니다. 이 규칙은 양측 모두가 반드시 준수해야 합니다.

### [제1장] API 명세 (설계도)

**제1조 (기술)**: 모든 API 명세는 OpenAPI (Swagger) v3.x 표준을 따른다.

**제2조 (프로세스)**:
- **"설계도 우선 (Design-First)"**: 코드를 작성하기 전, `docs/openapi/v1.yaml` 파일에 API 명세(Endpoint, Request, Response)를 '먼저' 정의한다.
- FastAPI는 이 '설계도'를 기반으로 `/docs` 엔드포인트를 자동 생성하여 '살아있는 문서'를 제공한다.
- 프론트엔드는 이 '설계도'를 기반으로 API 클라이언트 코드(예: React Query 훅)를 자동 생성(Code-gen)하는 것을 권장한다.

### [제2장] 데이터 교환 (언어)

**제1조 (형식)**: 모든 HTTP Body는 JSON을 사용한다.

**제2조 (명명 규칙)**: 모든 JSON Key는 **camelCase**를 사용한다. (예: `userId` O, `user_id` X)

### [제3장] 응답 및 에러 처리 (프로토콜)

**제1조 (표준 응답)**: 모든 성공/실패 응답은 '표준화된 구조'를 따른다.

- **성공 (2xx)**: 
  ```json
  {
    "data": { ... }
  }
  ```

- **실패 (4xx, 5xx)**: 
  ```json
  {
    "error": {
      "code": "E001",
      "message": "상세 에러 메시지"
    }
  }
  ```

---

## 디렉토리 구조

```
데일리모션_프로젝트/
├── 📜 AGENTS.md           # 1. 우리의 '헌법' (모든 규칙의 중심)
│
├── 🏗️ client/             # 2. 프론트엔드 (React Native)
│   ├── package.json
│   └── src/              # (곧 상세 설계 예정)
│
├── 🌳 server/             # 3. 백엔드 (FastAPI / Monolithic)
│   ├── requirements.txt
│   └── app/              # (모놀리식 서버 코드 위치)
│
└── 🤝 docs/               # 4. 문서화
    ├── MODULES.md        # (모듈별 상세 설계서)
    └── openapi/
        ├── v1.yaml       # (OpenAPI '설계도' 원본)
        └── generated/    # (설계도로 '자동 생성'된 파일들)
```

