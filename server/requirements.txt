# FastAPI 및 필수 의존성
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# HTTP 클라이언트
requests>=2.31.0
aiohttp>=3.9.0

# 데이터베이스 (PostgreSQL/PostGIS)
sqlalchemy>=2.0.0
alembic>=1.12.0
geoalchemy2>=0.14.0
# macOS (특히 Apple Silicon) 호환성을 위해 psycopg 사용 권장
# psycopg2-binary>=2.9.0  # Windows/Linux용
psycopg[binary]>=3.1.0  # 모든 플랫폼 호환 (권장)
# 또는 psycopg2-binary>=2.9.0  # macOS에서 문제 시 주석 해제

# 인증/보안
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# 테스트
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
